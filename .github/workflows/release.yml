name: Release Pipeline

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: read

jobs:
  validate-tag-signature:
    name: Validate Signed Tag
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout repository with tags
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
        with:
          fetch-depth: 0

      - name: Verify tag follows semantic format
        run: |
          case "${GITHUB_REF_NAME}" in
            v[0-9]*.[0-9]*.[0-9]*) ;;
            *)
              echo "Invalid release tag: ${GITHUB_REF_NAME}" >&2
              exit 1
              ;;
          esac

      - name: Verify tag signature
        run: git tag -v "${GITHUB_REF_NAME}"

  build-release-artifacts:
    name: Build Release Artifacts
    runs-on: ubuntu-24.04
    needs: [validate-tag-signature]
    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@0b93645e9fea7318ecaed2b359559ac225c90a2b  # v5.3.0
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install build and test dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.txt
          pip install build

      - name: Run unit and integration suites
        run: |
          pytest tests/unit tests/integration

      - name: Build wheel and sdist
        run: python -m build

      - name: Generate release notes from changelog metadata
        run: |
          python scripts/release/generate_release_notes.py \
            --version "${GITHUB_REF_NAME}" \
            --output release-notes.md

      - name: Upload dist artifacts
        uses: actions/upload-artifact@5d5d22a31266ced268874388b981cf7d8a2f934  # v4.3.1
        with:
          name: release-dist
          path: dist/

      - name: Upload generated release notes
        uses: actions/upload-artifact@5d5d22a31266ced268874388b981cf7d8a2f934  # v4.3.1
        with:
          name: release-notes
          path: release-notes.md

  publish-pypi:
    name: Publish PyPI (OIDC)
    runs-on: ubuntu-24.04
    needs: [build-release-artifacts]
    permissions:
      id-token: write
    steps:
      - name: Download dist artifacts
        uses: actions/download-artifact@fa0a91b85d4f404e444e00e005971372dc801d8  # v4.1.8
        with:
          name: release-dist
          path: dist/

      - name: Publish package distributions to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist/

  publish-github-release:
    name: Publish GitHub Release
    runs-on: ubuntu-24.04
    needs: [build-release-artifacts]
    permissions:
      contents: write
    steps:
      - name: Download dist artifacts
        uses: actions/download-artifact@fa0a91b85d4f404e444e00e005971372dc801d8  # v4.1.8
        with:
          name: release-dist
          path: dist/

      - name: Download release notes
        uses: actions/download-artifact@fa0a91b85d4f404e444e00e005971372dc801d8  # v4.1.8
        with:
          name: release-notes
          path: .

      - name: Create GitHub release with generated notes
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "${GITHUB_REF_NAME}" dist/* \
            --title "VoiceKey ${GITHUB_REF_NAME}" \
            --notes-file release-notes.md
