# VoiceKey PR Validation Pipeline
# Phase 0: Lint, Tests, Security Scans, Multi-Platform Matrix

name: PR Validation

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Least-privilege workflow permissions
permissions:
  contents: read
  id-token: write  # Required for OIDC/pypi trusted publishing in future release pipeline

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  PYTHON_VERSION: "3.11"
  # Disable interactive prompts during pip installs
  PIP_DISABLE_PIP_VERSION_CHECK: "1"

jobs:
  # =============================================================================
  # LINT - Code Quality Checks
  # =============================================================================
  lint:
    name: Lint
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@0b93645e9fea7318ecaed2b359559ac225c90a2b  # v5.3.0
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install ruff
        run: pip install ruff==0.8.4

      - name: Run ruff linter
        run: ruff check .

      - name: Run ruff formatter check
        run: ruff format --check .

  # =============================================================================
  # VULNERABILITY SCAN - Dependency Security
  # =============================================================================
  vulnerability-scan:
    name: Vulnerability Scan
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@0b93645e9fea7318ecaed2b359559ac225c90a2b  # v5.3.0
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install pip-audit
        run: pip install pip-audit==3.1.0

      - name: Create requirements file for audit
        run: |
          # Create a minimal requirements file for scanning
          # In production, this would come from pyproject.toml/requirements.txt
          echo "# VoiceKey dependencies - to be populated" > requirements-audit.txt

      - name: Run pip-audit
        run: pip-audit -r requirements-audit.txt || true
        # Allow pipeline to continue but report vulnerabilities
        # TODO: Change to fail on vulnerabilities after initial deps are added

  # =============================================================================
  # SECRET SCAN - TruffleHog
  # =============================================================================
  secret-scan:
    name: Secret Scan
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2

      - name: Run TruffleHog
        uses: trufflesecurity/trufflehog@8e2b6b78dd0dad62f4d6e4f4f78f4a5a8c0c1b6b  # v3.84.4
        with:
          path: .
          base: main
          head: HEAD
          # Disable scanning secrets in test fixtures
          extra_args: --exclude-faths="tests/fixtures,*.test.*"

  # =============================================================================
  # LICENSE COMPLIANCE - Scan
  # =============================================================================
  license-scan:
    name: License Compliance
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@0b93645e9fea7318ecaed2b359559ac225c90a2b  # v5.3.0
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install license-cop
        run: pip install license-cop==0.5.0

      - name: Run license-cop
        run: license-cop --fail-on=blacklist || true
        # TODO: Configure allowed licenses and fail appropriately

  # =============================================================================
  # UNIT TESTS - Matrix across OS and Python versions
  # =============================================================================
  unit-tests:
    name: Unit Tests (${{ matrix.os }} / Python ${{ matrix.python-version }})
    runs-on: ${{ matrix.os }}
    needs: [lint]
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-22.04, ubuntu-24.04, windows-2022]
        python-version: ["3.11", "3.12"]
        exclude:
          # Reduce matrix: skip Windows for Python 3.11 initially to save CI time
          - os: windows-2022
            python-version: "3.11"
    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@0b93645e9fea7318ecaed2b359559ac225c90a2b  # v5.3.0
        with:
          python-version: ${{ matrix.python-version }}
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest==8.3.4 pytest-cov==6.0.0
          # Install voicekey package for testing (when available)
          # pip install -e .

      - name: Run unit tests
        run: |
          pytest tests/unit \
            --cov=voicekey \
            --cov-report=xml \
            --cov-report=term-missing \
            -v

      - name: Upload coverage
        uses: actions/upload-artifact@5d5d22a31266ced268874388b981cf7d8a2f934  # v4.3.1
        with:
          name: coverage-${{ matrix.os }}-py${{ matrix.python-version }}
          path: coverage.xml
          retention-days: 7

  # =============================================================================
  # INTEGRATION SMOKE TESTS
  # =============================================================================
  integration-smoke:
    name: Integration Smoke (${{ matrix.os }} / Python ${{ matrix.python-version }})
    runs-on: ${{ matrix.os }}
    needs: [lint]
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-22.04, ubuntu-24.04, windows-2022]
        python-version: ["3.11", "3.12"]
    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@0b93645e9fea7318ecaed2b359559ac225c90a2b  # v5.3.0
        with:
          python-version: ${{ matrix.python-version }}
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest==8.3.4

      - name: Run integration smoke tests
        run: |
          pytest tests/integration \
            -v \
            --tb=short

      - name: Verify Python version compatibility
        run: |
          python --version
          python -c "import sys; print(f'Python {sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}')"

  # =============================================================================
  # TEST SUMMARY
  # =============================================================================
  test-summary:
    name: Test Summary
    runs-on: ubuntu-24.04
    needs: [unit-tests, integration-smoke]
    if: always()
    steps:
      - name: Download all coverage artifacts
        uses: actions/download-artifact@fa0a91b85d4f404e444e00e005971372dc801d8  # v4.1.8
        with:
          path: coverage-reports

      - name: Display test results
        run: |
          echo "## Test Results Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          # Check unit test results
          if ${{ needs.unit-tests.result == 'success' ]]; then
            echo " Unit Tests: PASSED" >> $GITHUB_STEP_SUMMARY
          else
            echo " Unit Tests: ${{ needs.unit-tests.result }}" >> $GITHUB_STEP_SUMMARY
          fi
          
          # Check integration test results
          if ${{ needs.integration-smoke.result == 'success' ]]; then
            echo " Integration Smoke: PASSED" >> $GITHUB_STEP_SUMMARY
          else
            echo " Integration Smoke: ${{ needs.integration-smoke.result }}" >> $GITHUB_STEP_SUMMARY
          fi
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Matrix Summary" >> $GITHUB_STEP_SUMMARY
          echo "- Ubuntu 22.04 + Python 3.11, 3.12" >> $GITHUB_STEP_SUMMARY
          echo "- Ubuntu 24.04 + Python 3.11, 3.12" >> $GITHUB_STEP_SUMMARY
          echo "- Windows 2022 + Python 3.12" >> $GITHUB_STEP_SUMMARY

  # =============================================================================
  # SECURITY SCANS SUMMARY
  # =============================================================================
  security-summary:
    name: Security Scans Summary
    runs-on: ubuntu-24.04
    needs: [vulnerability-scan, secret-scan, license-scan]
    if: always()
    steps:
      - name: Display security scan results
        run: |
          echo "## Security Scan Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          echo "### Vulnerability Scan" >> $GITHUB_STEP_SUMMARY
          if ${{ needs.vulnerability-scan.result == 'success' }}; then
            echo " pip-audit completed" >> $GITHUB_STEP_SUMMARY
          else
            echo "  pip-audit: ${{ needs.vulnerability-scan.result }}" >> $GITHUB_STEP_SUMMARY
          fi
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Secret Scan" >> $GITHUB_STEP_SUMMARY
          if ${{ needs.secret-scan.result == 'success' }}; then
            echo " TruffleHog: No secrets detected" >> $GITHUB_STEP_SUMMARY
          else
            echo " TruffleHog: ${{ needs.secret-scan.result }}" >> $GITHUB_STEP_SUMMARY
          fi
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### License Compliance" >> $GITHUB_STEP_SUMMARY
          if ${{ needs.license-scan.result == 'success' }}; then
            echo " License compliance: OK" >> $GITHUB_STEP_SUMMARY
          else
            echo "  License scan: ${{ needs.license-scan.result }}" >> $GITHUB_STEP_SUMMARY
          fi
